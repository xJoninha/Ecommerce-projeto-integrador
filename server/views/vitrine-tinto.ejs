<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <link rel="stylesheet" href="../stylesheets/vitrine-style.css">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vitrine</title>
</head>
    
<%- include('../views/partials/header'); -%>

<body>

    <h1 class="vitrine-text">Vinho Tinto</h1>

    <main>
        <section class="container-vitrine">
            <% for( let produto of result) { %>
                <section class="">

                    <div class="">
                        <a href=" /produtos/produto/<%= produto.id %> ">
                        <img src="<%= produto.image %> " alt=""></a>
                    </div>
                    <h3>
                        <%= produto.nome %></h3>
                    <p>R$ <%= produto.preco %></p>
                    
                    <!-- Botão de adicionar a Adega -->
                    <form method="post" action="/pedidos">
                    <input type="hidden" name="produtos_id" value="<%= produto.id %>" />
                    <input min="1" type="number" id="quantidade" name="quantidade" value="1"/>
                    <button type="submit" >Adicionar a Adega</button>
                    </form>
                    
                </section>
                <% } %>
            </section>
            
            <!-- <a class="botaoVoltar" href="/">Voltar à Página Inicial</a> -->
        </main>
        
        
        <%- include('../views/partials/footer'); -%>
    </body>
    </html>